# Core RAG stack
chromadb==1.5.9
sentence-transformers==5.4.1

# Multi-language parsing — tree-sitter >=0.22 individual packages.
# These build from source / have broad ABI support unlike tree-sitter-languages
# which tops out at cp312. Missing grammars fall back to line-chunking silently.
tree-sitter>=0.22.0
tree-sitter-python>=0.23.0
tree-sitter-javascript>=0.23.0
tree-sitter-typescript>=0.23.0
tree-sitter-go>=0.23.0
tree-sitter-rust>=0.23.0
tree-sitter-java>=0.23.0
tree-sitter-c>=0.23.0
tree-sitter-cpp>=0.23.0
tree-sitter-bash>=0.23.0
# Uncomment extras as needed:
# tree-sitter-ruby>=0.23.0
# tree-sitter-php>=0.23.0
# tree-sitter-kotlin>=0.23.0
# tree-sitter-lua>=0.23.0

# CodeBERT structural embeddings (optional — skip if disk/RAM is tight)
# CPU-only torch: uv pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers>=4.40.0
torch>=2.2.0

# API + frontend
fastapi==0.136.1
uvicorn==0.46.0
streamlit==1.57.0
pydantic==2.13.4

# Auth + database  (sqlite3 is stdlib — no install needed)
bcrypt>=4.0.0

# Git operations
GitPython==3.1.50

# Diagram OCR
easyocr==1.7.2

# HTTP
requests==2.33.1

# Evaluation suite
langchain-core==1.3.3
python-dotenv==1.2.2
pandas==3.0.2